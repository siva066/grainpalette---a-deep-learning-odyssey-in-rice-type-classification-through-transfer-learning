services:
  - type: web
    name: grain-palette
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run grain_palette_app.py --server.port=$PORT --server.address=0.0.0.0
